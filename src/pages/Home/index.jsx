import React, { Component } from "react";
import { Typography, Container } from "@mui/material";

export default (props) => {
  return (
    <Container sx={{ backgroundColor: "#e8f4f8" }}>
      <Typography></Typography>
    </Container>
  );
};
